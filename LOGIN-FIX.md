# Login fix

این نسخه مسیر ورود وب را اصلاح می‌کند.

## مشکل
صفحه `/profile` فقط فرم شماره همراه/OTP را نشان می‌داد. چون ورود پیامکی عمداً تا اتصال سرویس SMS غیرفعال شده بود، دکمه ورود نیز غیرفعال می‌شد؛ در حالی که API ورود با رمز عبور از قبل وجود داشت.

## تغییرات
- ورود اصلی وب: ایمیل یا نام کاربری + رمز عبور.
- `auth_login` اکنون فیلد `identifier` را می‌پذیرد و برای سازگاری `email`/`username` قدیمی را نیز قبول می‌کند.
- جستجوی حساب برای ورود رمزعبوری با email یا `admin_username` انجام می‌شود.
- نمایش/پنهان‌کردن رمز و ارسال فرم با Enter اضافه شد.
- خطاهای `invalid_credentials`، `rate_limited`، CSRF و session به پیام قابل فهم تبدیل شدند.
- ورود آزمایشی شماره همراه فقط وقتی `MAHDA_TEST_PHONE_AUTH` واقعاً فعال باشد، به‌صورت گزینه فرعی باقی می‌ماند.

## نکته
حساب‌هایی که قبلاً فقط با OTP ساخته شده‌اند و هیچ رمز عبوری برایشان تعیین نشده، طبیعتاً با رمز ناشناخته وارد نمی‌شوند. حساب‌های دارای password_hash واقعی (از جمله حساب‌های ایمیلی/مدیریتی) از همین فرم وارد می‌شوند. برای OTP عمومی بعداً سرویس SMS متصل می‌شود.
