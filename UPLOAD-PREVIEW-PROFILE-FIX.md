# اصلاح آپلود، پیش‌نمایش و ویترین

این نسخه روی ZIP جایگزین قبلی ساخته شده و سه اصلاح مشخص دارد.

## 1) ویترین / پروفایل

طراحی ویترین به نسخه قبل از بازطراحی اخیر برگردانده شده است. ساختار Hero جدید، گرید دو ستونه و Placeholderهای اضافه حذف شدند.

تنها اصلاح ظاهری پروفایل این است که آواتار با `aspect-ratio: 1/1`، عرض و ارتفاع ثابت و `border-radius: 999px` همیشه دایره واقعی باقی بماند و دیگر کشیده یا بیضی نشود.

## 2) آپلود عکس و ویدیو

- تصویر قبل از ارسال در مرورگر سبک می‌شود تا فشار روی PHP هاست اشتراکی و زمان Upload کم شود.
- هدف تصویر نهایی همچنان حدود 200KB است.
- اگر تصویر از قبل سبک باشد، سرور دیگر آن را بی‌دلیل decode/re-encode نمی‌کند.
- ویدیوی حداکثر 20MB حتی روی هاستی که `ffmpeg/exec` ندارد قابل Upload است.
- مدت ویدیو در مرورگر بررسی می‌شود و سقف 5 دقیقه حفظ شده است.
- اگر `ffprobe` روی سرور در دسترس باشد، مدت ویدیو سمت سرور نیز کنترل می‌شود.
- ویدیوی بیشتر از 20MB فقط وقتی پذیرفته می‌شود که ffmpeg روی سرور واقعاً در دسترس باشد تا Worker بتواند آن را به زیر 20MB برساند. روی هاست فاقد ffmpeg پیام واضح نمایش داده می‌شود و فایل در وضعیت خراب/نامشخص باقی نمی‌ماند.
- مسیرهای اختیاری `MAHDA_FFMPEG_PATH` و `MAHDA_FFPROBE_PATH` نیز پشتیبانی می‌شوند.

## 3) پیش‌نمایش مرحله 9

پیش‌نمایش ساده قبلی حذف شده و مرحله نهایی از ساختار بصری صفحه واقعی نمایش محتوا استفاده می‌کند:

- عنوان و آیکون دسته
- کاور 4:3
- آیکون‌های «پسندیدم» و «اجرا کردم»
- ناشر
- کارت‌های دسته، سن، زمان، هدف و تعداد نفرات
- آکاردئون‌های متن، هدف تربیتی، وسایل، مناسبت‌ها، فایل اصلی و فایل‌های همراه
- نمایش واقعی عکس، ویدیو و صوت داخل پیش‌نمایش
- نوار آیکون‌های ذخیره، اشتراک‌گذاری و محتواچین به صورت پیش‌نمایشی

تعاملات اجتماعی در Preview عمداً غیرفعال هستند چون محتوا هنوز منتشر نشده است.
